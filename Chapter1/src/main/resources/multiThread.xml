<?xml version="1.0" encoding="UTF-8" ?>

<suite name="thread" parallel="tests" thread-count="2">

    <!--
    tests级别：不同的test tag下的用例在不同的线程下执行
              相同的test tag下的用例只能在同一线程下执行

    classs级别：不同的class tag下的用例在不同的线程下执行
               相同的class tag下的用例只能在同一线程下执行

    methods级别：所有用例都可以在不同的线程下去执行
    thread-count：代表了最大并发线程数
    xml文件配置这种方法不能指定线程池，只有在方法里才能指定线程池
    -->

    <test name="demo1">
        <classes name="n1">
            <class name="com.course.testng.multiThread.MultiThreadOnXML"/>
        </classes>
        <classes name="n2">
            <class name="com.course.testng.multiThread.MultiThreadOnXML"/>
        </classes>
    </test>

    <test name="demo2">
        <classes>
            <class name="com.course.testng.multiThread.MultiThreadOnXML"/>
        </classes>
    </test>


</suite>